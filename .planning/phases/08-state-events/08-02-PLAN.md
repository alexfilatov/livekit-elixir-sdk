---
phase: 08-state-events
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - test/livekit/agents/events_test.exs
  - test/livekit/agents/user_state_machine_test.exs
  - test/livekit/agents/agent_state_machine_test.exs
  - test/livekit/agents/event_bus_test.exs
autonomous: true
requirements:
  - TEST-05

must_haves:
  truths:
    - "All UserStateMachine state transitions are exercised and verified in tests"
    - "All AgentStateMachine state transitions are exercised and verified in tests"
    - "All typed event structs are covered by unit tests"
    - ":telemetry event emission is captured and verified in tests"
    - "EventBus subscribe/publish/unsubscribe round-trip is verified"
    - "`mix test test/livekit/agents/events_test.exs test/livekit/agents/user_state_machine_test.exs test/livekit/agents/agent_state_machine_test.exs test/livekit/agents/event_bus_test.exs` exits 0"
  artifacts:
    - path: "test/livekit/agents/events_test.exs"
      provides: "Struct construction and pattern matching tests"
    - path: "test/livekit/agents/user_state_machine_test.exs"
      provides: "All UserStateMachine transitions, timer behaviour, and event emission tests"
    - path: "test/livekit/agents/agent_state_machine_test.exs"
      provides: "All AgentStateMachine transitions and invalid-transition rejection tests"
    - path: "test/livekit/agents/event_bus_test.exs"
      provides: "Registry pub/sub, :telemetry bridge, and emit_metric tests"
  key_links:
    - from: "user_state_machine_test.exs"
      to: "user_state_machine.ex"
      via: "start_link + speech_start/speech_end + get_state"
      pattern: "UserStateMachine\\.speech_start|UserStateMachine\\.speech_end"
    - from: "event_bus_test.exs"
      to: "event_bus.ex"
      via: "subscribe + publish + assert_receive"
      pattern: "EventBus\\.subscribe|EventBus\\.publish"
---

<objective>
Full ExUnit test suite covering all state machine transitions, typed event structs,
and EventBus pub/sub behaviour for Phase 8.

Purpose: Achieve 100% test coverage for all EVNT-0x requirements (TEST-05).
Output: Four test files, all green, no compiler warnings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/08-state-events/08-01-SUMMARY.md

<interfaces>
<!-- Modules created in Plan 08-01 — test against these contracts. -->

Livekit.Agents.Events — six nested structs:
  UserStateChanged{from, to, timestamp}
  AgentStateChanged{from, to, timestamp}
  SpeechCreated{text, confidence, timestamp}
  ConversationItemAdded{role, content, timestamp}
  ErrorEvent{stage, reason, timestamp}
  TelemetryMeasurement{metric, value, metadata, timestamp}

Livekit.Agents.UserStateMachine:
  start_link(opts)       — :away_timeout_ms, :event_bus
  speech_start(pid)      — cast
  speech_end(pid)        — cast
  get_state(pid)         — call, returns :listening | :speaking | :away
  get_metrics(pid)       — call, returns map()

Livekit.Agents.AgentStateMachine:
  start_link(opts)       — :event_bus
  set_state(pid, state)  — cast, :listening | :thinking | :speaking
  get_state(pid)         — call, returns :initializing | :listening | :thinking | :speaking
  get_metrics(pid)       — call, returns map()

Livekit.Agents.EventBus:
  start_link()           — starts Registry
  subscribe(session_id)  — registers current process
  unsubscribe(session_id)
  publish(session_id, event)
  emit_metric(session_id, metric, value)
  stop()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Events struct tests and UserStateMachine tests</name>
  <files>
    test/livekit/agents/events_test.exs,
    test/livekit/agents/user_state_machine_test.exs
  </files>
  <behavior>
    Events struct tests:
    - All six structs can be constructed with explicit fields and pattern-matched
    - %UserStateChanged{from: :listening, to: :speaking} matches correctly
    - %AgentStateChanged{from: :initializing, to: :listening} matches correctly
    - %SpeechCreated{text: "hello"} has text field accessible
    - %ConversationItemAdded{role: :user, content: "hi"} matches on role
    - %ErrorEvent{stage: :stt, reason: :timeout} matches on stage
    - %TelemetryMeasurement{metric: :ttft_ms, value: 150} matches on metric

    UserStateMachine tests:
    - Initial state is :listening after start_link
    - speech_start transitions :listening -> :speaking
    - speech_end transitions :speaking -> :listening
    - speech_start from :away transitions :away -> :speaking
    - speech_end followed by away_timeout_ms elapses transitions :speaking -> :away
      (use a short away_timeout_ms: 50 in test opts to avoid long waits)
    - speech_start during :speaking cancels the pending away timer (no :away transition)
    - get_metrics returns a map with :transitions key
    - When event_bus pid is provided, UserStateChanged is published on each transition
      (subscribe to the EventBus and assert_receive {:livekit_event, %UserStateChanged{}})
  </behavior>
  <action>
Write test files using ExUnit. Each test module uses `use ExUnit.Case, async: true`.

**test/livekit/agents/events_test.exs:**

```elixir
defmodule Livekit.Agents.EventsTest do
  use ExUnit.Case, async: true

  alias Livekit.Agents.Events
  alias Events.{UserStateChanged, AgentStateChanged, SpeechCreated,
                ConversationItemAdded, ErrorEvent, TelemetryMeasurement}

  describe "UserStateChanged" do
    test "can be constructed and pattern-matched" do
      event = %UserStateChanged{from: :listening, to: :speaking, timestamp: DateTime.utc_now()}
      assert %UserStateChanged{from: :listening, to: :speaking} = event
    end
  end

  # ... similar describes for all six structs
end
```

**test/livekit/agents/user_state_machine_test.exs:**

For the away timeout test, pass `away_timeout_ms: 50` in start_link opts and use
`Process.sleep(100)` after speech_end to allow the timer to fire, then call get_state.

For the event_bus integration test: start EventBus.start_link first (use a unique
Registry name per test via on_exit cleanup or accept that tests share the Registry).
Because Registry is registered by name, use `setup` to start EventBus if not running,
subscribe, run actions, assert_receive. Use `assert_receive {:livekit_event, %UserStateChanged{}}, 500`.

Pattern for tests that need EventBus:
```elixir
setup do
  # Start Registry for EventBus if not already started
  case Livekit.Agents.EventBus.start_link() do
    {:ok, _pid} -> :ok
    {:error, {:already_started, _}} -> :ok
  end
  :ok
end
```
  </action>
  <verify>
    <automated>cd /Users/alex/Projects/my/livekit/livekit && mix test test/livekit/agents/events_test.exs test/livekit/agents/user_state_machine_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both test files pass. All UserStateMachine transition scenarios covered. All six
    event structs have at least one test. No warnings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentStateMachine tests and EventBus tests</name>
  <files>
    test/livekit/agents/agent_state_machine_test.exs,
    test/livekit/agents/event_bus_test.exs
  </files>
  <behavior>
    AgentStateMachine tests:
    - Initial state is :initializing after start_link
    - set_state(:listening) from :initializing transitions to :listening
    - set_state(:thinking) from :listening transitions to :thinking
    - set_state(:speaking) from :thinking transitions to :speaking
    - set_state(:listening) from :speaking transitions to :listening (turn complete)
    - set_state(:listening) from :thinking transitions to :listening (interruption)
    - set_state(:speaking) from :speaking is an invalid transition — state stays :speaking
      (verify with get_state still returning :speaking after the invalid cast)
    - get_metrics returns a map with :transitions key
    - When event_bus pid provided, AgentStateChanged is published on each valid transition

    EventBus tests:
    - subscribe + publish: subscriber process receives {:livekit_event, event}
    - multiple subscribers on same session_id all receive the event
    - unsubscribe stops delivery (publish after unsubscribe not received, with assert_receive timeout)
    - emit_metric/3 publishes TelemetryMeasurement to subscribers
    - :telemetry bridge: attach handlers, execute a pipeline :telemetry event, subscriber
      receives TelemetryMeasurement (verify by subscribing and then calling
      :telemetry.execute([:livekit, :agents, :pipeline, :llm_first_token], %{role: :assistant}, %{monotonic_time: System.monotonic_time()})
      then assert_receive {:livekit_event, %TelemetryMeasurement{metric: :ttft_ms}})
    - publish to session_id with no subscribers is a no-op (no crash)
  </behavior>
  <action>
Write test files using ExUnit with `use ExUnit.Case, async: false` for EventBus tests
(Registry is globally named; use async: false to avoid race conditions between test
processes sharing the Registry).

**test/livekit/agents/agent_state_machine_test.exs:** `async: true` is fine (GenServers
are per-test and not globally named).

For invalid transition test: after casting set_state(:speaking) while already :speaking,
call get_state — it should still be :speaking. Use Process.sleep(50) after cast to let
the GenServer process the cast before calling get_state.

**test/livekit/agents/event_bus_test.exs:** `async: false`

Use a module-level setup_all or test-level setup that starts EventBus:
```elixir
setup do
  case Livekit.Agents.EventBus.start_link() do
    {:ok, _pid} -> :ok
    {:error, {:already_started, _}} -> :ok
  end
  # Use unique session_id per test to avoid cross-test interference
  session_id = "test-#{:erlang.unique_integer([:positive])}"
  %{session_id: session_id}
end
```

For the unsubscribe test, use `refute_receive {:livekit_event, _}, 100` after
unsubscribing and publishing.

For the telemetry bridge test, the EventBus telemetry handlers are attached once in
start_link. The test just needs to be a subscriber (or use the default broadcast
mechanism) and execute a telemetry event. Check if the TelemetryMeasurement is
published to the session or broadcast — match the actual implementation from Plan 08-01.
If the telemetry handler publishes to a well-known broadcast session_id (e.g., `:all`),
subscribe to that. If it publishes to `:all` session, subscribe to `:all`.

Note: The telemetry bridge implementation in EventBus publishes to all Registry entries
using `Registry.dispatch` with key `:all`. Adjust tests to match that: subscribe with
`EventBus.subscribe(:all)` or whatever session_id the handler dispatches to.
  </action>
  <verify>
    <automated>cd /Users/alex/Projects/my/livekit/livekit && mix test test/livekit/agents/agent_state_machine_test.exs test/livekit/agents/event_bus_test.exs --no-start 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both test files pass. All AgentStateMachine transitions and invalid-transition
    rejection are covered. EventBus subscribe/publish/unsubscribe/telemetry-bridge
    all tested. No warnings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> GenServer under test | Test sends casts/calls to started GenServers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Denial of Service | Shared Registry in async: false tests | mitigate | Use unique session_id per test; async: false prevents races on the named Registry |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/alex/Projects/my/livekit/livekit
mix test test/livekit/agents/events_test.exs \
         test/livekit/agents/user_state_machine_test.exs \
         test/livekit/agents/agent_state_machine_test.exs \
         test/livekit/agents/event_bus_test.exs
```

All tests pass (exit 0). Zero failures.

Also run:
```bash
mix test
```
to verify no regressions in prior phases.
</verification>

<success_criteria>
- All four test files pass: events, user_state_machine, agent_state_machine, event_bus
- UserStateMachine: 7+ test cases covering all transitions including timeout and cancellation
- AgentStateMachine: 6+ test cases covering all transitions and invalid transition rejection
- EventBus: 5+ test cases covering subscribe, publish, unsubscribe, multi-subscriber, telemetry bridge
- `mix test` (full suite) exits 0 with no regressions
- No compiler warnings in test files
</success_criteria>

<output>
After completion, create `.planning/phases/08-state-events/08-02-SUMMARY.md` following
the summary template.
</output>
