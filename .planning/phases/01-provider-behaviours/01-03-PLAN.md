---
phase: 01-provider-behaviours
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - test/livekit/agents/stt_behaviour_test.exs
  - test/livekit/agents/tts_behaviour_test.exs
  - test/livekit/agents/llm_behaviour_test.exs
  - test/livekit/agents/vad_behaviour_test.exs
autonomous: true
requirements:
  - TEST-01
  - BEHV-01
  - BEHV-02
  - BEHV-03
  - BEHV-04
  - BEHV-05

must_haves:
  truths:
    - "`mix test test/livekit/agents/` passes with no failures"
    - "Stub modules for all four behaviours compile without Elixir compiler warnings about missing callbacks"
    - "Each stub's `capabilities/0` returns a map containing all required keys for that provider type"
    - "Default `validate_config/1` injected by `__using__` returns `:ok` and is testable without overriding"
    - "`mix coveralls` shows 100% coverage for the four new behaviour files"
  artifacts:
    - path: "test/livekit/agents/stt_behaviour_test.exs"
      provides: "Conformance tests for Livekit.Agents.STT"
      contains: "StubSTT, transcribe/2 test, capabilities/0 test, validate_config/1 test"
    - path: "test/livekit/agents/tts_behaviour_test.exs"
      provides: "Conformance tests for Livekit.Agents.TTS"
      contains: "StubTTS, synthesize/2 test, capabilities/0 test, validate_config/1 test"
    - path: "test/livekit/agents/llm_behaviour_test.exs"
      provides: "Conformance tests for Livekit.Agents.LLM"
      contains: "StubLLM, chat/2 test, capabilities/0 test, validate_config/1 test, LLMChunk struct test"
    - path: "test/livekit/agents/vad_behaviour_test.exs"
      provides: "Conformance tests for Livekit.Agents.VAD"
      contains: "StubVAD, stream/1 capabilities/0 test, validate_config/1 test, VADEvent struct test"
  key_links:
    - from: "test/livekit/agents/stt_behaviour_test.exs"
      to: "Livekit.Agents.STT"
      via: "use Livekit.Agents.STT in StubSTT"
      pattern: "use Livekit.Agents.STT"
    - from: "test/livekit/agents/llm_behaviour_test.exs"
      to: "Livekit.Agents.LLM.LLMChunk"
      via: "alias + struct assertion"
      pattern: "alias Livekit.Agents.LLM.LLMChunk"
    - from: "test/livekit/agents/vad_behaviour_test.exs"
      to: "Livekit.Agents.VAD.VADEvent"
      via: "alias + struct assertion"
      pattern: "alias Livekit.Agents.VAD.VADEvent"
---

<objective>
Write conformance-stub test files for all four provider behaviours defined in Plans 01 and 02.
Each test file defines a minimal stub module that implements the behaviour, then asserts correct
return shapes, required capability keys, and default `validate_config/1` behaviour.

Purpose: Achieves 100% test coverage (TEST-01) for the four new behaviour source files.
Compiler-enforced behaviour conformance is tested implicitly: if a required callback is missing,
the stub definition fails to compile and the test suite errors.

Output: Four test files in `test/livekit/agents/`
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<!-- Prior plan outputs — these are the modules under test -->
@.planning/phases/01-provider-behaviours/01-01-SUMMARY.md
@.planning/phases/01-provider-behaviours/01-02-SUMMARY.md

<!-- Existing test file pattern to follow -->
@test/livekit/agents/audio_frame_test.exs

<interfaces>
<!-- Contracts under test — from Plan 01 -->

Livekit.Agents.STT required callbacks:
  transcribe(audio :: binary(), opts :: keyword()) :: {:ok, SpeechEvent.t()} | {:error, error_reason()}
  capabilities() :: %{streaming: boolean(), interim_results: boolean(), diarization: boolean(), languages: [String.t()]}
  [optional] stream(config :: map()) :: {:ok, pid()} | {:error, error_reason()}
  [optional, default :ok] validate_config(config :: map()) :: :ok | {:error, error_reason()}

Livekit.Agents.STT.SpeechEvent fields:
  type: :start | :interim | :final | :end
  text: String.t() (default "")
  confidence: float() (default 0.0)
  language: String.t() (default "en")

Livekit.Agents.TTS required callbacks:
  synthesize(text :: String.t(), opts :: keyword()) :: {:ok, binary()} | {:error, error_reason()}
  capabilities() :: %{streaming: boolean(), voices: [String.t()], audio_formats: [audio_format()], word_timing: boolean()}
  [optional] stream(config :: map()) :: {:ok, pid()} | {:error, error_reason()}
  [optional, default :ok] validate_config(config :: map()) :: :ok | {:error, error_reason()}

Livekit.Agents.LLM required callbacks:
  chat(chat_context :: term(), opts :: keyword()) :: {:ok, chat_message()} | {:error, error_reason()}
  capabilities() :: %{streaming: boolean(), tool_calling: boolean(), vision: boolean(), max_context_tokens: pos_integer() | nil}
  [optional] stream(chat_context :: term(), opts :: keyword()) :: {:ok, pid()} | {:error, error_reason()}
  [optional, default :ok] validate_config(config :: map()) :: :ok | {:error, error_reason()}

Livekit.Agents.LLM.LLMChunk fields:
  type: :text | :tool_call | :done
  content: term() (default nil)

Livekit.Agents.VAD required callbacks:
  stream(config :: map()) :: {:ok, pid()} | {:error, error_reason()}   # NOT optional
  capabilities() :: %{realtime: boolean(), speech_probability: boolean()}
  [optional, default :ok] validate_config(config :: map()) :: :ok | {:error, error_reason()}

Livekit.Agents.VAD.VADEvent fields:
  type: :speech_start | :speech_end | :inference
  probability: float() (default 0.0)
  frames: [AudioFrame.t()] (default [])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write STT and TTS conformance tests</name>
  <files>
    test/livekit/agents/stt_behaviour_test.exs,
    test/livekit/agents/tts_behaviour_test.exs
  </files>
  <action>
Create two test files. Follow the pattern from `test/livekit/agents/audio_frame_test.exs`:
- `use ExUnit.Case, async: true` at top
- Descriptive test names
- No `describe` blocks needed (flat structure is fine for conformance tests)

**File 1: `test/livekit/agents/stt_behaviour_test.exs`**

```
defmodule Livekit.Agents.STTTest do
  use ExUnit.Case, async: true

  alias Livekit.Agents.STT
  alias Livekit.Agents.STT.SpeechEvent

  defmodule StubSTT do
    use Livekit.Agents.STT

    @impl true
    def transcribe(_audio, _opts) do
      {:ok, %SpeechEvent{type: :final, text: "hello world", confidence: 0.99, language: "en"}}
    end

    @impl true
    def capabilities do
      %{streaming: false, interim_results: false, diarization: false, languages: ["en", "es"]}
    end
  end
  ...tests...
end
```

Required tests (write all of these):
1. `"StubSTT.transcribe/2 returns {:ok, SpeechEvent}"` — call with `<<1, 2, 3>>` and `[]`, assert `{:ok, event}`, assert `event.type == :final`, `event.text == "hello world"`, `event.confidence == 0.99`, `event.language == "en"`
2. `"SpeechEvent defaults are correct"` — `event = %SpeechEvent{}`, assert `event.text == ""`, `event.confidence == 0.0`, `event.language == "en"`, `event.type == nil`
3. `"capabilities/0 returns map with all required STT keys"` — call `StubSTT.capabilities()`, assert map has keys `:streaming`, `:interim_results`, `:diarization`, `:languages` (use `Map.has_key?/2`)
4. `"default validate_config/1 returns :ok"` — `assert :ok == StubSTT.validate_config(%{api_key: "test"})`
5. `"SpeechEvent struct accepts all valid type atoms"` — assert `%SpeechEvent{type: :start}`, `%SpeechEvent{type: :interim}`, `%SpeechEvent{type: :final}`, `%SpeechEvent{type: :end}` all build without error

**File 2: `test/livekit/agents/tts_behaviour_test.exs`**

```
defmodule Livekit.Agents.TTSTest do
  use ExUnit.Case, async: true

  alias Livekit.Agents.TTS

  defmodule StubTTS do
    use Livekit.Agents.TTS

    @impl true
    def synthesize(_text, _opts) do
      {:ok, <<0, 1, 2, 3>>}
    end

    @impl true
    def capabilities do
      %{streaming: false, voices: ["default"], audio_formats: [:pcm], word_timing: false}
    end
  end
  ...tests...
end
```

Required tests:
1. `"StubTTS.synthesize/2 returns {:ok, binary}"` — call with `"hello"` and `[]`, assert `{:ok, audio}`, assert `is_binary(audio)`
2. `"capabilities/0 returns map with all required TTS keys"` — assert map has keys `:streaming`, `:voices`, `:audio_formats`, `:word_timing`
3. `"capabilities voices is a list"` — `caps = StubTTS.capabilities()`, assert `is_list(caps.voices)`
4. `"capabilities audio_formats is a list"` — assert `is_list(caps.audio_formats)`
5. `"default validate_config/1 returns :ok"` — `assert :ok == StubTTS.validate_config(%{api_key: "test"})`

Both files must use `async: true`. Do NOT start any real processes. All tests are pure function calls — no GenServer, no process spawning.
  </action>
  <verify>
    <automated>mix test test/livekit/agents/stt_behaviour_test.exs test/livekit/agents/tts_behaviour_test.exs --no-start 2>&1</automated>
  </verify>
  <done>
    Both test files exist. `mix test test/livekit/agents/stt_behaviour_test.exs test/livekit/agents/tts_behaviour_test.exs` exits 0 with all tests passing (0 failures). No compiler warnings about missing behaviour callbacks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write LLM and VAD conformance tests, then verify full coverage</name>
  <files>
    test/livekit/agents/llm_behaviour_test.exs,
    test/livekit/agents/vad_behaviour_test.exs
  </files>
  <action>
Create two more test files with the same pattern.

**File 1: `test/livekit/agents/llm_behaviour_test.exs`**

```
defmodule Livekit.Agents.LLMTest do
  use ExUnit.Case, async: true

  alias Livekit.Agents.LLM
  alias Livekit.Agents.LLM.LLMChunk

  defmodule StubLLM do
    use Livekit.Agents.LLM

    @impl true
    def chat(_chat_context, _opts) do
      {:ok, %{role: :assistant, content: "Hello!"}}
    end

    @impl true
    def capabilities do
      %{streaming: false, tool_calling: false, vision: false, max_context_tokens: 4096}
    end
  end
  ...tests...
end
```

Required tests:
1. `"StubLLM.chat/2 returns {:ok, chat_message map}"` — call with `[]` and `[]`, assert `{:ok, msg}`, assert `msg.role == :assistant`, `msg.content == "Hello!"`
2. `"chat/2 accepts term() as chat_context"` — call with `"any term"`, `%{messages: []}`, and `nil` — all must return `{:ok, _}` (confirming `term()` typing works)
3. `"capabilities/0 returns map with all required LLM keys"` — assert map has keys `:streaming`, `:tool_calling`, `:vision`, `:max_context_tokens`
4. `"capabilities max_context_tokens is a positive integer or nil"` — `caps = StubLLM.capabilities()`, assert `is_integer(caps.max_context_tokens) or is_nil(caps.max_context_tokens)`
5. `"default validate_config/1 returns :ok"` — `assert :ok == StubLLM.validate_config(%{})`
6. `"LLMChunk struct has correct defaults"` — `chunk = %LLMChunk{}`, assert `chunk.type == nil`, `chunk.content == nil`
7. `"LLMChunk struct accepts all valid type atoms"` — assert `%LLMChunk{type: :text, content: "hi"}`, `%LLMChunk{type: :tool_call, content: %{}}`, `%LLMChunk{type: :done}` all build without error

**File 2: `test/livekit/agents/vad_behaviour_test.exs`**

Note: VAD `stream/1` is a required callback (not optional). The stub must implement it.
The stub implementation should return a pid — spawn a process that does nothing, just to satisfy the contract.

```
defmodule Livekit.Agents.VADTest do
  use ExUnit.Case, async: true

  alias Livekit.Agents.VAD
  alias Livekit.Agents.VAD.VADEvent
  alias Livekit.Agents.AudioFrame

  defmodule StubVAD do
    use Livekit.Agents.VAD

    @impl true
    def stream(_config) do
      pid = spawn(fn -> receive do :stop -> :ok end end)
      {:ok, pid}
    end

    @impl true
    def capabilities do
      %{realtime: true, speech_probability: false}
    end
  end
  ...tests...
end
```

Required tests:
1. `"StubVAD.stream/1 returns {:ok, pid}"` — call with `%{}`, assert `{:ok, pid}`, assert `is_pid(pid)`. After assert, send `:stop` to pid to clean up: `send(pid, :stop)`.
2. `"capabilities/0 returns map with all required VAD keys"` — assert map has keys `:realtime`, `:speech_probability`
3. `"capabilities realtime is boolean"` — `caps = StubVAD.capabilities()`, assert `is_boolean(caps.realtime)`
4. `"default validate_config/1 returns :ok"` — `assert :ok == StubVAD.validate_config(%{})`
5. `"VADEvent struct has correct defaults"` — `event = %VADEvent{}`, assert `event.type == nil`, `event.probability == 0.0`, `event.frames == []`
6. `"VADEvent struct accepts all valid type atoms"` — assert `%VADEvent{type: :speech_start}`, `%VADEvent{type: :speech_end}`, `%VADEvent{type: :inference}` all build without error
7. `"VADEvent frames field accepts a list of AudioFrame structs"` — `frame = %AudioFrame{}`, `event = %VADEvent{type: :speech_end, frames: [frame]}`, assert `length(event.frames) == 1`

After writing all four test files, verify full suite and coverage:

```bash
mix test test/livekit/agents/
mix coveralls
```

If `mix coveralls` shows any new behaviour file with less than 100% coverage, add additional tests to cover the missing lines and re-run.
  </action>
  <verify>
    <automated>mix test test/livekit/agents/llm_behaviour_test.exs test/livekit/agents/vad_behaviour_test.exs --no-start 2>&1</automated>
  </verify>
  <done>
    Both test files exist. `mix test test/livekit/agents/llm_behaviour_test.exs test/livekit/agents/vad_behaviour_test.exs` exits 0 with all tests passing. `mix test test/livekit/agents/` passes (all agent tests pass, no regressions). `mix coveralls` shows 100% coverage for `lib/livekit/agents/stt.ex`, `lib/livekit/agents/tts.ex`, `lib/livekit/agents/llm.ex`, and `lib/livekit/agents/vad.ex`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Test files only. All stub implementations are compile-time-only definitions with no external I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | StubVAD spawns a process | accept | The test spawns a minimal process and sends `:stop` in the same test to trigger its exit. Process leaks would only occur if the test crashed after spawn — ExUnit handles cleanup of test process trees. Risk is negligible in async tests. |

No other security threats apply to test-only code with stub implementations.
</threat_model>

<verification>
Full phase gate checks:

```bash
# All four behaviour files compile cleanly
mix compile --warnings-as-errors

# All agent tests pass (no regressions)
mix test test/livekit/agents/

# Credo passes on all four new source files
mix credo --strict lib/livekit/agents/stt.ex lib/livekit/agents/tts.ex lib/livekit/agents/llm.ex lib/livekit/agents/vad.ex

# Format check
mix format --check-formatted lib/livekit/agents/stt.ex lib/livekit/agents/tts.ex lib/livekit/agents/llm.ex lib/livekit/agents/vad.ex

# 100% coverage gate
mix coveralls
```

All commands must exit 0. Coverage must show 100% for all four new behaviour source files.
</verification>

<success_criteria>
- All four test files exist and have zero failing tests
- Each test file defines a stub module using `use Behaviour` — no compiler warnings about missing callbacks
- STT test: 5 tests covering transcribe/2, SpeechEvent struct, capabilities keys, validate_config default
- TTS test: 5 tests covering synthesize/2, capabilities keys (incl. voices/formats as lists), validate_config default
- LLM test: 7 tests covering chat/2 with term() context, LLMChunk struct, capabilities keys, validate_config default
- VAD test: 7 tests covering stream/1 returns pid, VADEvent struct, capabilities keys, validate_config default, frames field
- `mix test test/livekit/agents/` passes with 0 failures
- `mix coveralls` shows 100% for all four new `lib/livekit/agents/*.ex` behaviour files
- No regressions in the existing test suite (`mix test` passes overall)
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-behaviours/01-03-SUMMARY.md` using the summary template.
</output>
